<table class="table is-fullwidth">
  <thead>
    <tr>
      <th>Category</th>
      <th>Description</th>
      <th colspan="2">Coordinates (Latitude, Longitude)</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {{#each waterfall.POIs}}
    <tr class="row">
      <td class="type">
        {{type}}
      </td>
      <td class="description">
        {{description}}
      </td>
      <td class="coordinates" colspan="2">{{latitude}}, {{longitude}}</td>
      <td>
        {{#if ../waterfall.canModify}}
        <a href="/waterfall/{{../waterfall._id}}/viewpoi/{{_id}}" class="ui icon button">
          <i class="fas fa-edit"></i>
        </a>
        <a href="/waterfall/{{../waterfall._id}}/deletepoi/{{_id}}" class="ui icon button">
          <i class="fas fa-trash"></i>
        </a>
        {{/if}}
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>